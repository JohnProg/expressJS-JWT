Rest API using ExpressJS and JWT
================================

Steps:

npm install

Create a file call "config.js" with the following:

module.exports = {
	'secret': 'YOUR_SECRET_KEY',
	'api_settings': 'YOUR_API_SETTINGS'
};